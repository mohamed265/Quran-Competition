import javax.swing.JOptionPane;

import com.qurancompetition.views.MainWindow;

public class Main {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		new MainWindow();
		// new NewContestant();
		// new Competition();
		// DataBase.getConnection();
		// new RegisterInCompetition();
		// DataBase.close();

		// int start = DataBase.getStartLevel(1);
		// Object[] possibilities = new String[30 - start];
		// for (int i = 0; i < possibilities.length; i++) {
		// start++;
		// possibilities[i] = start + "";
		// }
		// String s = (String) JOptionPane.showInputDialog(null, "اختر عدد
		// الاجزاء",
		// "عدد الاجزاء التى سيشارك بها فى المسابقة", JOptionPane.PLAIN_MESSAGE,
		// null, possibilities, null);
		// System.out.println(s);

		// String s = (String) JOptionPane.showInputDialog(null, "اختر عدد
		// الاجزاء",
		// "عدد الاجزاء التى سيشارك بها فى المسابقة", JOptionPane.PLAIN_MESSAGE,
		// null, null, null);
	}

}
